# Changelog

## 1.0.0 - 2026-09-21

- OCR 扩展为三种可选方案：默认 Windows OCR、可一键安装的 PP-OCRv6 Small 本地插件，以及自备 API Key 的云端视觉 OCR。
- Windows OCR 增加小字自动放大、灰度对比度增强双路识别和显式语言选择。
- PP-OCRv6 Small 模型从 PaddleOCR 官方地址下载，安装前校验文件大小与 SHA-256，并支持一键卸载。
- 云端 OCR 推荐阿里云百炼 `qwen3.5-ocr`，使用独立 Base URL、模型和 Windows 凭据，不与翻译 API Key 混用。
- OCR 原文可在悬浮窗直接修正后重新翻译。
- 修复 Alt+W 首次打开时同步加载本地模型运行时导致选区层空白并占用桌面的问题；PP-OCR 改为按需加载，并增加 Esc、Alt+W 再按一次和失焦自动退出三层保护。
- 设置文件增加显式 Schema 版本，旧配置首次升级时保留对应源版本的迁移备份。
- 设置页支持导出、复制和校验恢复不含 API Key 的 JSON 备份。
- 新增 GitHub Releases 正式版本检查；发现新版本时自动复制可信下载页地址。
- 新增可手动触发或由版本标签触发的 Windows 正式安装包构建流水线。
- 新增统一发布脚本、SHA-256 清单和正式发布检查清单。
- 完成 v1.0 稳定版文档、隐私边界和迁移测试。

## 0.6.0 - 2026-09-20

- 新增 Windows 本地 OCR 截图翻译，默认独立快捷键为 `Alt + W`。
- OCR 选区覆盖层自动跟随鼠标所在显示器，并支持高 DPI 坐标换算。
- 大尺寸截图会在保持比例的情况下缩小到 Windows OCR 支持的范围。
- OCR 图片仅在本机内存中处理，不写入磁盘或上传；只有识别文字会交给已配置 Provider 翻译。
- OCR 翻译悬浮窗同时显示完整识别文字与译文，并支持分别复制。
- 旧设置自动迁移 `ocrShortcut`，并禁止 OCR 与划词翻译快捷键重复。

## 0.5.0 - 2026-09-20

- 设置页新增 OpenAI、阿里云百炼、DeepSeek、Ollama、LM Studio 和自定义 Provider 预设。
- 切换预设时自动填写对应 Base URL 与推荐模型，仍可手动修改。
- 本机回环地址的 Ollama/LM Studio 可在不配置 API Key 的情况下使用。
- 仅在 API Key 非空时发送 Bearer Authorization 请求头。
- 翻译结果和历史记录会显示实际选用的 Provider 名称。

## 0.4.0 - 2026-09-20

- 新增本地翻译历史窗口，支持搜索、收藏、复制和删除。
- 托盘菜单可直接打开翻译历史，最近显示 200 条记录。
- 收藏记录在历史顶部显示，并在缓存淘汰时优先保留。
- 悬浮窗支持固定显示、复制原文、复制译文和绕过缓存重新翻译。
- 增加 `Ctrl+C`、`Ctrl+Shift+C`、`Alt+R` 和 `Esc` 键盘操作。
- SQLite 启动时自动迁移历史收藏字段，并覆盖旧版本数据库升级测试。

## 0.3.1 - 2026-09-20

- 新请求会主动取消旧的选词读取与网络请求，减少无效流量和等待。
- 设置、快捷键、开机启动和 API Key 作为一个事务保存，失败时恢复旧配置。
- 配置文件使用同目录临时文件安全替换，并可从备份恢复。
- Provider Base URL 强制使用 HTTPS；仅本机回环地址允许 HTTP。
- 设置页新增不包含 API Key 的可复制诊断信息。

## 0.3.0 - 2026-09-20

- 修复高 DPI、窗口调整和多显示器场景下翻译悬浮窗超出工作区的问题。
- API 限流、超时、连接失败和服务端临时故障自动短暂重试。
- 连续触发翻译时更早丢弃过期请求。
- 翻译缓存限制为 1000 条，并在设置页提供一键清理。
- 新增 Windows CI 和安全回滚脚本。

## 0.2.0 - 2026-08-31

- 首个公开安装版本。
- 支持全局快捷键、托盘、悬浮翻译、Windows 凭据管理器和 SQLite 缓存。
- 支持点击外部自动收起与可选开机启动。
